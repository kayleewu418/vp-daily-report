# VP 日报脚本新手快速操作指南

这份指南适合第一次使用脚本、没有 Git 和 Python 经验的同学。

完成一次准备后，每天只需要：

1. 把当天 Excel 放进数据文件夹
2. 打开 PowerShell
3. 复制一段命令并按回车
4. 在 PowerShell 中查看文案

## 一、安装 Python

1. 打开 Python 官网：[https://www.python.org/downloads/](https://www.python.org/downloads/)
2. 点击下载最新版 Python 3
3. 运行安装程序
4. 安装界面中勾选 `Add python.exe to PATH`
5. 点击 `Install Now`

安装完成后，打开 PowerShell，输入：

```powershell
python --version
```

如果显示类似 `Python 3.13.7`，说明安装成功。脚本需要 Python 3.10 或更高版本。

## 二、在 D 盘创建文件夹

在电脑 D 盘创建一个 `VP日报` 文件夹，并在里面创建 `工具` 和 `数据` 两个文件夹：

```text
D:\VP日报
├─ 工具
└─ 数据
```

用途：

- `D:\VP日报\工具`：存放从 GitHub 下载的脚本
- `D:\VP日报\数据`：每天存放最新的 Excel 日报

## 三、从 GitHub 下载脚本

1. 打开项目页面：[https://github.com/kayleewu418/vp-daily-report](https://github.com/kayleewu418/vp-daily-report)
2. 点击绿色的 `Code` 按钮
3. 点击 `Download ZIP`
4. 下载完成后，右键 ZIP 文件，选择“全部解压缩”
5. 把解压后的文件夹移动到 `D:\VP日报\工具`
6. 将文件夹名称改成 `vp-daily-report`

完成后的脚本路径应该是：

```text
D:\VP日报\工具\vp-daily-report\VP日报自动化\scripts\calc_vp_daily_report.py
```

如果找不到这个文件，通常是文件夹多套了一层。请打开文件夹确认上面的路径结构。

## 四、保存每天的 Excel 日报

把刷新并保存好的日报 Excel 放进：

```text
D:\VP日报\数据
```

文件名必须使用下面的格式：

```text
VP日报_YYYYMMDD.xlsx
```

例如，2026 年 9 月 20 日运行的日报应命名为：

```text
VP日报_20260920.xlsx
```

文件名日期代表运行日期，脚本会自动分析前一天的数据。因此，`VP日报_20260920.xlsx` 输出的是 9 月 19 日的数据。

Excel 中必须保留下面四个工作表，名称不能修改：

- `销售过程数据走势`
- `线上官渠潜客客流`
- `各车系当日数据`
- `月累环比 (2)`

脚本按照文件名中的八位日期选择最新文件，不是按照文件最后修改时间选择。因此，不要保留日期更晚但数据尚未更新的测试文件。

运行脚本前，请先在 Excel 中刷新数据、确认公式计算完成，然后保存并关闭文件。

## 五、每天运行脚本

1. 点击 Windows 开始菜单
2. 搜索并打开 `PowerShell`
3. 复制下面整段命令
4. 粘贴到 PowerShell 后按回车

```powershell
$VpRoot = 'D:\VP日报'
$ProjectDir = Join-Path $VpRoot '工具\vp-daily-report'
$ReportDir = Join-Path $VpRoot '数据'

Set-Location $ProjectDir

[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONIOENCODING = 'utf-8'

$latest = Get-ChildItem $ReportDir -Filter 'VP日报_*.xlsx' |
    Where-Object { $_.BaseName -match '^VP日报_\d{8}$' } |
    Sort-Object Name -Descending |
    Select-Object -First 1

if ($null -eq $latest) {
    throw "$ReportDir 中没有找到日报文件"
}

python -X utf8 `
    '.\VP日报自动化\scripts\calc_vp_daily_report.py' `
    $latest.FullName `
    --stdout-only
```

### 这段命令在做什么

不用背诵这段命令，日常使用时整段复制即可。它主要完成四件事：确定文件夹位置、避免中文乱码、找到日期最新的日报，以及运行 Python 脚本。

#### 1. 确定工具和数据文件夹

```powershell
$VpRoot = 'D:\VP日报'
$ProjectDir = Join-Path $VpRoot '工具\vp-daily-report'
$ReportDir = Join-Path $VpRoot '数据'
Set-Location $ProjectDir
```

- `$VpRoot` 是日报总文件夹，即 `D:\VP日报`。
- `Join-Path` 用来安全地拼接文件夹路径。
- `$ProjectDir` 最终代表 `D:\VP日报\工具\vp-daily-report`。
- `$ReportDir` 最终代表 `D:\VP日报\数据`。
- `Set-Location` 相当于进入项目文件夹，也就是 PowerShell 中常见的 `cd` 命令。

#### 2. 避免中文乱码

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONIOENCODING = 'utf-8'
```

这两行让 PowerShell 和 Python 都使用 UTF-8 编码，避免中文文件名和输出文案变成乱码。

#### 3. 自动寻找最新日报

```powershell
$latest = Get-ChildItem $ReportDir -Filter 'VP日报_*.xlsx' |
    Where-Object { $_.BaseName -match '^VP日报_\d{8}$' } |
    Sort-Object Name -Descending |
    Select-Object -First 1
```

- `Get-ChildItem`：查看数据文件夹中的文件。
- `'VP日报_*.xlsx'`：先找以 `VP日报_` 开头的 Excel 文件，`*` 表示任意内容。
- `Where-Object`：进一步要求文件名必须是 `VP日报_` 加八位日期，例如 `VP日报_20260920.xlsx`。
- `$_`：表示当前正在检查的那个文件；`.BaseName` 表示不带 `.xlsx` 后缀的文件名。
- `Sort-Object Name -Descending`：按照文件名从大到小排列。日期采用 `YYYYMMDD` 格式，所以最新日期会排在最前面。
- `Select-Object -First 1`：只取排在第一位的文件，并保存到 `$latest`。
- 每行末尾的 `|` 叫作“管道符”，表示把这一行的结果交给下一行继续处理。

#### 4. 检查文件并运行脚本

```powershell
if ($null -eq $latest) {
    throw "$ReportDir 中没有找到日报文件"
}

python -X utf8 `
    '.\VP日报自动化\scripts\calc_vp_daily_report.py' `
    $latest.FullName `
    --stdout-only
```

- 如果没有找到符合命名规则的日报，`throw` 会显示错误并停止运行。
- `python -X utf8` 表示用 UTF-8 模式启动 Python。
- `calc_vp_daily_report.py` 是实际负责计算日报的程序。
- `$latest.FullName` 是最新日报的完整文件路径。
- `--stdout-only` 表示只在 PowerShell 窗口中显示结果，不额外生成 CSV 文件。
- Python 命令行末尾的反引号 `` ` `` 表示命令还没有结束，下一行仍属于同一条命令。反引号后不要添加空格。

脚本会自动选择文件名日期最新的日报，并直接在 PowerShell 中显示：

- `概览解析-AI颜色版`
- `车系数据解析-AI颜色版`

不会额外生成 CSV 文件。

## 六、参考输出示例

运行成功后，PowerShell 中会显示类似下面的文案。以下数字仅用于展示格式，实际结果以当天 Excel 为准；“前一日”“上周末日均”等比较周期由脚本根据日期自动选择。

```text
概览解析-AI颜色版

线上官渠潜客客流：官渠客流有增长，VS 上周末日均 +10.8%；

门店客流：门店客流下降，VS 上周末日均 -4.1%；

线索总量：线索总量下降，VS 上周末日均 -5.2%；G9L 线索4,959（占线索总量比例17.7%），VS 上周末日均 +55.4%；全系（非 G9L）线索 VS 上周末日均 -12.5%；其中 GX -11.9%，L03 -13.3%，其他车型合计 -12.2%；

试驾总量：试驾总量下降，VS 上周末日均 -6.7%；G9L 试驾2,322（占试驾总量比例19.7%），VS 上周末日均 +12.2%；L03 -11.2%，GX -6.9%，其他车型合计 -10.8%；

锁单总量：全系锁单总量1,875台，VS 上周末日均 +15.7%；其中 G9L 净锁单336台（占全系锁单比例17.9%），上周末日均暂无可比数据；L03 净锁单637台，-11.2%；GX 净锁单151台，-7.6%；其他车型合计751台，+1.5%。

车系数据解析-AI颜色版

客流：19日客流30,482，月累客流339,143，月累环比-16.5%，月累同比-7.9%
线索：19日线索28,461，月累线索539,969，月累环比-3.9%，月累同比+13.5%
试驾：19日试驾11,792，月累试驾140,081，月累环比-0.5%，月累同比-9.5%
锁单：19日锁单1,875，月累锁单21,355，月累环比-30.6%，月累同比-5.1%
转化率：月累转化率4.0%，月累环比-0.8%，月累同比-0.7%
```

## 七、每天最简操作

完成前面的安装和文件夹设置后，每天只做下面三步：

1. 刷新并保存当天的 Excel
2. 将 Excel 放入 `D:\VP日报\数据`
3. 打开 PowerShell，粘贴“第五步”的命令并按回车

数据更新后，不需要删除昨天的 Excel。脚本会按照文件名自动选择日期最新的文件。

## 八、常见问题

### 提示“python 不是内部或外部命令”

Python 没有安装成功，或者安装时没有勾选 `Add python.exe to PATH`。重新安装 Python，并勾选这个选项。

### 提示“找不到路径”

检查下面两个路径是否真实存在：

```text
D:\VP日报\工具\vp-daily-report
D:\VP日报\数据
```

同时确认脚本文件位于：

```text
D:\VP日报\工具\vp-daily-report\VP日报自动化\scripts\calc_vp_daily_report.py
```

### 提示“没有找到日报文件”

检查 Excel 是否放在 `D:\VP日报\数据`，并确认文件名类似：

```text
VP日报_20260920.xlsx
```

不要在文件名后增加“最终版”“修改版”等文字。

### 输出的日期不是今天

日报文件名日期代表运行日期，脚本分析的是前一天。这是正常逻辑。

### 输出的数据还是旧数据

脚本读取的是 Excel 已经保存的数据。请重新打开 Excel，刷新数据、等待公式计算完成，然后保存文件再运行。

### 中文显示乱码

使用本指南第五步的完整命令运行，不要删除其中的 UTF-8 设置。

### 脚本提示缺少某个日期或指标

说明 Excel 中对应工作表还没有更新完整。请先检查并刷新：

- `线上官渠潜客客流`
- `销售过程数据走势`
- `各车系当日数据`
- `月累环比 (2)`

刷新并保存 Excel 后，再运行一次脚本。

## 九、更新 GitHub 上的新版本

需要更新脚本时，重新打开 GitHub 项目页面，下载最新 ZIP，并用新文件夹替换：

```text
D:\VP日报\工具\vp-daily-report
```

`D:\VP日报\数据` 中的 Excel 不需要移动或删除。
